#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{
  int y;
  if (argc != 3)
    {
      printf("Error\n");
	return (1);
    }
  
    
 
  y = atoi(argv[1]) * atoi(argv[2]);
	   printf("%d\n", y);
	   return (0);
}
