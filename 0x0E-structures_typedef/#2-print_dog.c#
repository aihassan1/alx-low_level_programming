#include <stdio.h>
#include "dog.h"

void print_dog(struct dog *d)
{
  if (d == NULL)
    printf(" ");

  if (d->name == NULL || d->owner == NULL)
    printf("nill");

  printf("Name: %s\n   e: %f\n Owner: %s\n", d->name, d->age, d->owner);
}
